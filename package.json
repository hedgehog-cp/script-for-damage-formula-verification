{
  "name": "script-for-damage-formula-verification",
  "version": "1.0.0",
  "description": "ダメージ検証用スプレのためのGoogle Apps Script (GAS).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && cp ./appsscript.json ./dist/",
    "push": "npm run build && clasp push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/google-apps-script": "^1.0.83",
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3"
  }
}
