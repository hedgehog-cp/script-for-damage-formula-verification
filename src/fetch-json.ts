// 権限設定が困難のため未使用.
/**
 * urlが指すリソースのcontent textをjsonとしてparseし, これを型Tの値として返します.
 * @param url
 * @returns {T} parseされたjson
 */
// function fetchJson<T>(url: string): T {
//   return JSON.parse(UrlFetchApp.fetch(url).getContentText()) as T;
// }
